services:
  milcom:
    build: .
    container_name: milcom-dashboard
    restart: unless-stopped
    ports:
      - "5050:5050"
    environment:
      - PI_IP=172.16.16.35
      - AUTH_USER=admin
      - AUTH_PASS=milcom2026
